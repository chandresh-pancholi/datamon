apiVersion: v1
kind: Pod
metadata:
  # This name uniquely identifies the Deployment
  name: datamon-dev-debug
spec:
  containers:
    - name: dev
      image: golang:1.11-alpine
      tty: true
      stdin: true
      volumeMounts:
        - name: credentials
          readOnly: true
          mountPath: "/etc/datamon-creds"
        - name: thankyoumsau42
          readOnly: true
          mountPath: "/data"
  volumes:
    - name: credentials
      secret:
        secretName: gcs-credentials
    - name: thankyoumsau42
      persistentVolumeClaim:
        claimName: thankyoumsau42
        readOnly: true


